# DevilsPie

Set transparency on VS Code in arch:

```
pacman -S xcompmgr transset-df devilspie
mkdir ~/.devilspie
cp vscode.ds ~/.devilspie   # copy vscode.ds file in this folder into dir
```

**Note:** Might need to start xcompmgr first
